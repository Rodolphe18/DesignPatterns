PROXY PATTERN

- Virtual Proxy

It allows us to delay the creation of a heavy object until it is actually needed

- Protection Proxy

It controls access to the real object by performing authorization or permission checks before allowing operations

- Remote Proxy

It provides a local representative for a class that exists on a remote server therby handling communication between the client and the remote class